---
'squareone': patch
---

Bump node from 22.21.1-alpine to 22.23.2-alpine in /apps/squareone in the docker group across 1 directory. `.nvmrc` and the root `engines.node` constraint are updated to match, so local development and CI use the same Node.js version as the production image.
