---
'squareone': patch
'@lsst-sqre/squared': patch
'@lsst-sqre/file-factory': patch
---

Update the Node.js runtime from 22 to the 24 LTS line (24.21.0). The Docker image, GitHub Actions workflows (via `.nvmrc`), the devcontainer, the `engines` field, and `@types/node` all move together.
