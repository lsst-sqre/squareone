---
"squareone": patch
---

Add a dev-only `/dev` auth control panel (login/persona/scope toggles), mock `/auth/api/v1/login`, and exclude all dev mocks from production builds via `.dev` route files.
