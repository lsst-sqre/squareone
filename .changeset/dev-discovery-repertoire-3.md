---
'squareone': patch
---

The development Repertoire discovery route (`/api/dev/repertoire/discovery`) now serves the Repertoire 3.0.0 discovery shape from the updated `mockDiscovery`, without changes to the route itself. The shape includes the environment metadata, service titles, docs URLs, required scopes, and quota labels, and the mock adds a `prompt` dataset.
