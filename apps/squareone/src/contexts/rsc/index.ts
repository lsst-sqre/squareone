/**
 * RSC-specific React contexts for the App Router.
 *
 * This module provides context providers and hooks optimized for React
 * Server Components and the App Router architecture.
 */

export { ConfigProvider, useConfig } from './ConfigProvider';
