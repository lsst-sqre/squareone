/**
 * Type exports for Times Square client.
 *
 * Re-exports types inferred from Zod schemas for convenient importing.
 */

export type {
  ContentNode,
  ErrorDetail,
  ErrorModel,
  ExecutionStatus,
  FormattedText,
  GitHubCheckRunConclusion,
  GitHubCheckRunStatus,
  GitHubCheckRunSummary,
  GitHubContentsRoot,
  GitHubContributor,
  GitHubNodeType,
  GitHubPr,
  GitHubPrContents,
  GitHubPrState,
  GitHubSourceMetadata,
  HTTPValidationError,
  HtmlEvent,
  HtmlStatus,
  Metadata,
  Page,
  PageSummary,
  Person,
  ValidationError,
} from './schemas';
