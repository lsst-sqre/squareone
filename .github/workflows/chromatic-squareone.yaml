name: 'Chromatic: Squareone'

'on':
  push:
    branches-ignore:
      - 'dependabot/**'
      - 'renovate/**'
      # Transient branch created by changesets/action v2 while it updates
      # changeset-release/main; it is deleted seconds after the push.
      - 'changesets-ghcommit-temp/**'
    tags-ignore:
      - '*'

permissions:
  contents: read # Required by called workflow

jobs:
  call:
    if: false
    uses: ./.github/workflows/run-chromatic.yaml
    with:
      path: apps/squareone
      workspaceFilter: squareone
    secrets:
      token: ${{ secrets.SQUAREONE_CHROMATIC_TOKEN }}
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
